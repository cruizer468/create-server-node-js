const fs = require('fs');
const path = require('path');

// fs.mkdir(path.join(__dirname,'Test'), {}, (err)=>{
//     if(err) throw err;
//     console.log('created folder sucsesfly');
// })

// creat file and write to it 

// fs.writeFile(path.join(__dirname,'/Test', 'Hello.txt'),'hello world!',
//      (err) => {
//          if(err) throw err;
//          console.log('file written to it ...');
//      }
// )

// append to file content

// fs.appendFile(path.join(__dirname,'/Test', 'Hello.txt'),'leern node!',
//      (err) => {
//          if(err) throw err;
//          console.log('appended ...');
//      }
// )

// reading file content

// fs.readFile(path.join(__dirname,'/Test','hello.txt'), 'utf8',
//    (err, data) => {
//     if(err) throw err;
//     console.log('reading', data);
//    }
// )

// fs.rename(path.join(__dirname,'/Test', 'Hello.txt'),
//              path.join(__dirname,'/Test', 'new.txt'),
//      (err) => {
//          if(err) throw err;
//          console.log('renamed ...');
//      }
// )

